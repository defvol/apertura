.form-wrapper
  .container
    = haml :form

.container.results
  %p.lead= t('poll.results.header')

  #chart(class="#{session[:color]}")
  %p.legend= t('poll.results.legend')

  %p.poll-action-wrapper
    %a(href="/datatron" id="poll-reboot" class="#{session[:color]}")= t('poll.actions.reboot')

= erb :donut

